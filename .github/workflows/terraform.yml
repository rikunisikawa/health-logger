name: Terraform

on:
  pull_request:
    branches: [main]
    paths:
      - "terraform/**"
      - "migrations/athena/**"

# PR で同一ブランチへの新しい push が来たら古いランをキャンセルする
concurrency:
  group: terraform-${{ github.ref }}
  cancel-in-progress: true

env:
  TF_VERSION: "1.12.0"
  AWS_REGION: ap-northeast-1

jobs:
  terraform-prod:
    name: "Terraform Prod"
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    defaults:
      run:
        working-directory: terraform/envs/prod

    steps:
      - uses: actions/checkout@v6

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_PROD }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init -reconfigure

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -var='lambda_s3_keys={"create_record":"placeholder","get_latest":"placeholder","push_subscribe":"placeholder","push_notify":"placeholder","weekly_push_notify":"placeholder","get_item_config":"placeholder","save_item_config":"placeholder","delete_record":"placeholder","get_env_data":"placeholder","get_env_data_latest":"placeholder","get_summary":"placeholder","aggregate_daily":"placeholder","export_records":"placeholder","get_correlation":"placeholder"}' \
            -no-color -out=tfplan
        env:
          TF_VAR_vapid_private_key: ${{ secrets.VAPID_PRIVATE_KEY_PROD }}

      - name: Comment PR with plan
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const botComment = comments.find(
              (c) => c.user.type === 'Bot' && c.body.includes('Terraform Plan (prod)')
            );
            const output = `#### Terraform Plan (prod)
            \`\`\`
            ${{ steps.plan.outputs.stdout }}
            \`\`\``;
            if (botComment) {
              github.rest.issues.updateComment({
                owner: context.repo.owner, repo: context.repo.repo,
                comment_id: botComment.id, body: output,
              });
            } else {
              github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: context.issue.number, body: output,
              });
            }

