name: health_logger
version: '1.0.0'
config-version: 2
profile: health_logger
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  health_logger:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
